https://www.codewars.com/kata/5983cba828b2f1fd55000114/train/javascript

function oddOne(arr) {
    if (arr.length === 0 || arr.reduce((a, b) => a + b, 0) % 2 === 0) {
        return -1;
    }
    return arr.findIndex(el => el % 2 !== 0);
}
