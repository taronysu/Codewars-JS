https://www.codewars.com/kata/5d50e3914861a500121e1958/train/javascript

function addLetters(...letters) {
  let l = String.fromCharCode(letters.map(el => el.charCodeAt()-96).reduce((a,b)=>a+b,0)%26+96)
  return l === '`' ? 'z' : l;
}
